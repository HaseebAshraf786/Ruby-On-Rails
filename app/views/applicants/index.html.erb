<div  style="margin:0% 10% 0% 10%;padding:5px 0px 5px 0px;">
<% @applicants.each do |app|%>
<div style = "border:2px solid green;margin-top:5px; margin-bottom:2px;padding:30px;background-color:
#d5dbdb ;">
    <h4>Applicant Name</h4>
    <%= Profile.find(app.profile_id).name%><br>
    <%= button_to "Hire", "/profiles/#{params[:profile_id]}/posts/#{params[:post_id]}/applicants/#{app.id}" ,method: :get%>
    </div>
<%end%>
</div>